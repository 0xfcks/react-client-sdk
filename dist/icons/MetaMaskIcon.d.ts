import React from 'react';
export interface IconProps {
    onClick?: () => void;
    className?: string;
    size?: number;
}
export declare const MetaMaskIcon: ({ className, onClick, size, }: IconProps) => React.ReactElement;
